using ImportData;

namespace Tests.EDocs
{
    public class Contracts
    {
/*
        [Fact]
        public void ContractsImport()
        {
            Program.Main(TestSettings.GetArgsContracts());

            var result = true;
            foreach (var expectedContract in TestData.EDocs.Contracts.GetContracts())
            {
                var exceptionList = new List<Structures.ExceptionsStruct>();
                var actualContract = BusinessLogic.GetEntityWithFilter<IContracts>(x => x.Name == expectedContract.Name, exceptionList, TestSettings.Logger, true);

                result = result && expectedContract == actualContract;
            }
            Assert.True(result);
        }
*/
    }
}
